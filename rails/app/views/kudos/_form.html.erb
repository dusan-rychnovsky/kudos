<%= simple_form_for(kudo) do |f| %>

  <%= f.input :from %>
  <%= f.input :to %>
  <%= f.input :for %>

  <%= f.button :submit, (kudo[:id] ? "Update " : "Create ") + "Kudos", class: "btn-primary" %>
<% end %>
