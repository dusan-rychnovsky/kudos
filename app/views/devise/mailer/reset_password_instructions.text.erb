Hello <%= @resource.username %>,

Someone has requested a link to change your password at KudosCounter v2.
You can do this through the link below:

  <%= edit_password_url(@resource, reset_password_token: @token) %>

The link will stay active within the next 24 hours.
If you didn't request this, please ignore this email.

Sincerely,
Dusan Rychnovsky
