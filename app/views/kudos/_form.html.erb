<%= simple_form_for(kudo) do |f| %>

  <% selected = to_id ? { selected: to_id } : {} %>
  <%= f.association :to, label_method: :username, **selected %>
  <%= f.input :for %>

  <%= f.button :submit, "Give Kudos", class: "btn-primary" %>
<% end %>
